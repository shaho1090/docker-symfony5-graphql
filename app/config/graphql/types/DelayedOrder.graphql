type DelayedOrder {
    id:ID!
    order: Order!
    agent: User
    delayReport: DelayReport
    state: String!
    createdAt: String!
    description: String
}

input AssignDelayedOrder{
    id: ID!
    agentId: ID!
}

input AutoAssignDelayedOrder{
    agentId: ID!
}